# Copy to .env and fill in. Never commit .env.
ANTHROPIC_API_KEY=sk-ant-...
# Model used by the agent (default: claude-opus-5)
MODEL_ID=claude-opus-5
# Optional: enable Anthropic server-side refusal fallback (beta). Default off.
ENABLE_REFUSAL_FALLBACK=false
